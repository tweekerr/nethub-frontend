import React, {FC} from 'react';
import FilledDiv from "../UI/FilledDiv";

interface IPublicDashboardProps {
}

const PublicDashboard: FC<IPublicDashboardProps> = () => {
  return (
    <FilledDiv>

    </FilledDiv>
  );
};

export default PublicDashboard;
